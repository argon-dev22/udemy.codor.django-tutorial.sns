{
  "name": "sample", // devcontainer name
  "service": "sample-service", // service name in docker-compose.yml
  "workspaceFolder": "/app", // WORKDIR in Dockerfile
  "dockerComposeFile": "../../compose.yml", // docker-compose.yml file path
  "customizations": {
    "vscode": {
      "extensions": [
        // Add extensions here
      ]
    }
  }
}

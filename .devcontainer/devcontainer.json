{
  "name": "udemy_coder_django-tutorial_sns", // devcontainer name
  "service": "app", // service name in docker-compose.yml
  "workspaceFolder": "/app", // WORKDIR in Dockerfile
  "dockerComposeFile": "../compose.yml", // docker-compose.yml file path
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.debugpy",
        "kevinrose.vsc-python-indent"
      ]
    }
  }
}
